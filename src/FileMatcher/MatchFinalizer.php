<?php declare(strict_types=1);

namespace Srter\FileMatcher;

class MatchFinalizer {

    public function applyMatches(Match ...$match) 
    {
       // move the srtfile getting its path to the videofilename minus the extension
       // to the same videfile path
    }
}